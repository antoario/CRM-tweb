import { CrudBaseDirective } from './crud-base.directive';

describe('CrudBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new CrudBaseDirective();
    expect(directive).toBeTruthy();
  });
});
